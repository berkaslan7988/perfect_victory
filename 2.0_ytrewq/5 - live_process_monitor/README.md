# Live Process Monitor

Lists and continuously monitors running processes (PID, name, resource usage) in
real time — useful for spotting suspicious activity.

```bash
pip install psutil
python live_process_monitor.py
```
