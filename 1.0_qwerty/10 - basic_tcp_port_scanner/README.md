# Basic TCP Port Scanner

Scans a target host for open TCP ports using raw sockets — an introduction to
network reconnaissance.

```bash
python basic_tcp_port_scanner.py
```

> Scan only hosts you own or are authorized to test — see ../../DISCLAIMER.md.
