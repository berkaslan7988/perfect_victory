# IP OSINT Information Gatherer

Collects open-source intelligence about an IP address (geolocation, ISP, etc.)
using public lookup services.

```bash
python ip_osint_information_gatherer.py
```

> Use only on IPs you are authorized to investigate.
