# Phishing Detection (ML)

A machine-learning classifier (scikit-learn) that learns to distinguish phishing
messages from legitimate ones based on extracted features.

```bash
pip install scikit-learn pandas
python phishing_detection.py
```
