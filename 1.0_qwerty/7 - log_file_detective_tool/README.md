# Log File Detective Tool

Parses server logs (`server_logs.txt` sample included) to surface suspicious
events such as repeated failed logins or unusual access patterns.

```bash
python log_file_detective_tool.py
```
