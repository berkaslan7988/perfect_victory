# E-Commerce (Flask + DB)

An online shop with a product catalog, shopping cart, and admin panel. Classic
MVC structure with `templates/` and `static/`.

```bash
pip install flask
python database_setup.py   # create + seed the database
python app.py
# open http://127.0.0.1:5000
```
