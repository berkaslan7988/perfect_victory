# Job Board (Flask + DB)

A job-listings board where postings are stored in a database and rendered through
Flask templates.

```bash
pip install flask
python init_db.py          # initialize the database
python app.py
# open http://127.0.0.1:5000
```
