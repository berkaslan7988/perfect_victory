# Network Anomaly Detection (ML)

Uses scikit-learn to model normal network behavior and flag anomalous traffic
that deviates from the learned baseline.

```bash
pip install scikit-learn pandas
python network_anomaly_detection.py
```
