# News Site (Flask + DB)

A dynamic news website backed by a database. Articles are stored in and served
from a DB; templates live in `templates/`, assets in `static/`.

```bash
pip install flask
python database_setup.py   # create + seed the database
python app.py
# open http://127.0.0.1:5000
```
