# Forum (Flask + DB)

A discussion forum with threads and posts persisted to a database — the most
feature-complete web project of the level.

```bash
pip install flask
python init_db.py          # initialize the database
python app.py
# open http://127.0.0.1:5000
```
