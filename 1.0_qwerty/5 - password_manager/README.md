# Password Manager

A simple local vault that stores and retrieves credentials. Introduces the idea
of centralized secret storage. (Generated `vault.txt` is git-ignored.)

```bash
python password_manager.py
```
